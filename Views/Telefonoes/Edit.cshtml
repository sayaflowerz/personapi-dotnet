@model personapi_dotnet.Models.Entities.Telefono

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Telefono</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Num" />
            <div class="form-group">
                <label asp-for="Oper" class="control-label"></label>
                <input asp-for="Oper" class="form-control" />
                <span asp-validation-for="Oper" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Duenio" class="control-label"></label>
                <select asp-for="Duenio" class="form-control" asp-items="ViewBag.Duenio"></select>
                <span asp-validation-for="Duenio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div class="nav-box">
    <a asp-action="Index" class="nav-link">Volver a la lista</a>
</div>

<style>
    .nav-box {
        background-color: #e0f7fa; /* Color de fondo claro */
        padding: 10px; /* Espaciado */
        border-radius: 5px; /* Bordes redondeados */
        margin-top: 20px; /* Espaciado superior */
    }

    .nav-link {
        color: white; /* Color de texto blanco */
        padding: 10px 15px; /* Espaciado interno */
        border-radius: 5px; /* Bordes redondeados para enlaces */
        background-color: #007bff; /* Azul */
        text-decoration: none; /* Sin subrayado */
    }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
